{
  "name": "radarolx",
  "version": "1.0.0",
  "description": "RadarWeb OLX Frontend (Firebase Hosting)",
  "scripts": {
    "build:ui": "cd frontend && npm run build",
    "deploy": "firebase deploy"
  },
  "dependencies": {
    "firebase-functions": "^7.0.6",
    "firebase-admin": "^13.7.0"
  },
  "devDependencies": {
    "firebase-functions-test": "^3.4.1"
  }
}